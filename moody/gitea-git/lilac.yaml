maintainers:
  - github: moodyhunter

pre_build_script: aur_pre_build(maintainers='kageru')

build_prefix: moody-x86_64
post_build: aur_post_build

update_on:
  - source: aur
    aur: gitea-git
    strip_release: true

