# yaml-language-server: $schema=../../lilac-yaml-schema.yaml

maintainers:
  - github: moodyhunter

build_prefix: extra-x86_64

pre_build: vcs_update
post_build: git_pkgbuild_commit

update_on:
  - source: vcs
    vcs: git+https://gitea.com/gitea/act_runner.git
